// Lesson editor splitter geometry. Used by LessonClient to clamp the resizable
// pane between a readable minimum and a sensible maximum, and to nudge with
// arrow keys.
export const MIN_LEFT_PX = 280;
export const MAX_LEFT_RATIO = 0.75;
export const KEY_NUDGE_PX = 24;
